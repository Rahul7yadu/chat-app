"use client"

import Login from "@/components/containers/Login"
const page = () => {
  // const {isLoggedIn,loading} = useAuthContextValues()
 
  // const router = useRouter()
  // useEffect(()=>{
  //    if(!loading && isLoggedIn){
  //     return router.push('/')
  //    } 
  // },[])


  // if(loading) return <Loading/>
  
  
    return (
      <Login/>
    ) 
}
export default page